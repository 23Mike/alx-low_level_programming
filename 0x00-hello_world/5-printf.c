#include <stdio.h>
/**
 * main - print the strings in the printf function
 * Decription: prints "with proper grammar, but outcome is a piece of art,"
 * Return: 0
 */
int main(void)
{
printf("with proper grammar, but outcome is a piece of art,\n");
return (0);
}
